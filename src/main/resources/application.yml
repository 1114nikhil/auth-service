server:
  port: 8081

spring:
  application:
    name: auth-service

  datasource:
    url: jdbc:postgresql://localhost:3005/spring_auth
    username: postgres
    password: 123456

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: RYvF#k>}6jc-aO.eFbx)KS>pv^$.W_L
  expiration: 3600000
  refresh-expiration: 604800000
